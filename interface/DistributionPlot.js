
function PlotParameterDistribution(){
    // Generate distribution
	
	// Determine how the distribution will be plotted
	Coord=DetermineDistributionPlotPosition(Value.X, 0.003);

	// Plot the distribution using a x-y plot 
}



function DetermineDistributionPlotPosition(Value.X, MinDistance){
	//Takes in Value.X, Returns Value.ID, Value.X, Value.PlotX, Value.PlotY
	
	
	Level=0;
	PointsPlaced=0;
	MedianPoint=M
	while (PointsPlaced<TotalPoints){
		
		// Find the point that is closest to the median value of all the points 
		// Set that point
		// pushxcoord
		MinXPosThisLevel=//the value set at the central point
		MaxXPosThisLevel=//the value set at the central point
		PointsCanBePlaced=true;
		while (PointsPlaced<TotalPoints && PointsCanBePlaced==true){
			// Find the nearest point that is allowable on the first level - repeat until no longer possible
			PointsCanBePlaced=false;
			
			ShortestDistance=SomethingBig;
			for // each point in the remaining vector
				// find the shortest distance between the current point and the xcoord vector
				for each element of the xcoord
			// if the distance is shortest 
				// set the shortest distance to this distance
				//stor

		}
		Level++;
	}
	
	return Value;
}